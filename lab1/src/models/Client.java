package models;

public class Client {
    String client;
    int clientID;

    public Client(String client, int clientID) {
        this.client = client;
        this.clientID = clientID;
    }
}
